# Unreleased

Nothing yet. The changes that were here shipped in
[v0.10.0](v0.10.0.md).

Add user-visible changes, fixes, minimum Herdr versions, App compatibility, and
restart or migration instructions as they land. Before release, move them to the
chosen `vX.Y.Z.md` and record the validation actually performed.
