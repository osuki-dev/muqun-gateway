# Release notes

Commit `vX.Y.Z.md` before pushing the matching version tag. The release workflow
requires a nonempty file and publishes it as the GitHub Release description.
Include user-visible changes, fixes, minimum Herdr versions, App compatibility,
and restart or migration instructions. Validate every expected binary after
release. Do not publish a version unless the user requested a release.
