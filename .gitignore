# Build output
/target

# The gateway's own secrets live in the config/state dirs, never the repo,
# but ignore any that get symlinked or copied in during local testing.
*.log
devices.json
pairing.json
push-tokens.json
config.json

# OS / editor cruft
.DS_Store
*.swp
.idea/
.vscode/

# Subagent-driven-development scratch workspace
.superpowers/
